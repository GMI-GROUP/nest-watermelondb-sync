import { ParseTimestampPipe } from './parse-timestamp.pipe';

describe('ParseTimestampPipePipe', () => {
  it('should be defined', () => {
    expect(new ParseTimestampPipe()).toBeDefined();
  });
});
